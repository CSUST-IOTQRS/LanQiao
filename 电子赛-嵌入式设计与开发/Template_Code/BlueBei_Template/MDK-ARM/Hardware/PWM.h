#ifndef __PWM_H
#define __PWM_H

// 外部调用
extern uint16_t R39_Freq;
extern uint16_t R40_Freq;

// PWM初始化 --- 兼顾输出比较和输入捕获
void PWM_Init(void);

//==================================仅作为测试程序==================================
// 测试函数
void PWM_Demo(void);
//==================================仅作为测试程序==================================

#endif
